import Head from "next/head";
import Link from "next/link";

const siteTitle = "T3 Notes App";

export default function Layout({
  children,
  home,
}: {
  children: React.ReactNode;
  home?: boolean;
}) {
  return (
    <div
      //data-theme="dark"
      data-theme="business"
      className="flex min-h-screen flex-col"
    >
      <Head>
        <link rel="icon" href="/favicon.ico" />
        <meta name="description" content="Generated by create-t3-app" />
        {/* <meta
          property="og:image"
          content={`https://og-image.vercel.app/${encodeURI(
            siteTitle
          )}.png?theme=light&md=0&fontSize=75px&images=https%3A%2F%2Fassets.zeit.co%2Fimage%2Fupload%2Ffront%2Fassets%2Fdesign%2Fnextjs-black-logo.svg`}
        /> */}
        <meta name="og:title" content={siteTitle} />
        {/* <meta name="twitter:card" content="summary_large_image" /> */}
      </Head>
      <div className="container navbar  mx-auto px-0">
        <div className="px-4 py-6 sm:py-8">
          <p className="text-3xl font-extrabold tracking-tight sm:text-4xl ">
            T3 <span className="text-accent">Note</span> App
          </p>
        </div>
      </div>

      <main className="container mx-auto flex grow flex-col">{children}</main>
      <footer className="container mx-auto">
        {!home && (
          <Link href="/">
            <p className="px-4 pb-8 pt-4 text-xl underline">← Back to home</p>
          </Link>
        )}
      </footer>
    </div>
  );
}
