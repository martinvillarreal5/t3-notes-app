export type FolderMinimalInfo = {
  id: string;
  title: string;
};

/* export type NoteMinimalInfo = {
  id: string;
  title: string | null;
  content: string;
  createdAt: Date;
  updatedAt: Date;
};
 */
